var input = JSON.parse(require('fs').readFileSync('data.json', 'utf-8'));
input = String(input['1013']);
var lines = input.split('\n');

// Your code down here
lines = lines[0].split(` `);
console.log(lines);

function Maior(a, b, c){

 
}
Maior(lines[0],lines[1],lines[2])
